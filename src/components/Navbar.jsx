import { Link } from "react-router-dom";

const Navbar = () => {
  return (
    <nav>
      <ul>
        <li>
          <link to="/" />
        </li>
      </ul>
    </nav>
  );
};

export default Navbar;
